# Agente Residente ADES — dependencias Python
# Entorno: /opt/ades/.agent/venv  (ARM64, sin CUDA)
# Activar:  source /opt/ades/.agent/venv/bin/activate
# Instalar: pip install --no-cache-dir -r requirements.txt

# Embeddings semánticos (LongTermMemory + SemanticCache)
# fastembed usa ONNX runtime — compatible ARM64 sin GPU, 384 dims (all-MiniLM-L6-v2)
fastembed>=0.8.0

# Base de datos PostgreSQL + pgvector
psycopg2-binary>=2.9.10   # vector como str(list) + cast ::vector en SQL

# Numérico
numpy>=2.0.0

# Cache semántico en Valkey
redis>=5.0.0              # cliente compatible Valkey 9.x
