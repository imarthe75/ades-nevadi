# ADES Backend — FastAPI FASE 1
# Python 3.12

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Base de datos (async + sync para Celery tasks)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Validación y configuración
pydantic==2.10.4
pydantic-settings==2.7.0
pydantic[email]

# Autenticación OIDC/JWT
python-jose[cryptography]==3.3.0
httpx==0.28.1          # introspección JWKS de Authentik

# MinIO (almacenamiento S3)
minio==7.2.15

# Utilidades
python-multipart==0.0.20   # file uploads
openpyxl==3.1.5            # importación masiva Excel (.xlsx)
orjson==3.10.12            # JSON rápido
structlog==24.4.0          # logging estructurado

# Tareas async (Celery — worker separado)
celery[redis]==5.4.0
redis==5.2.1               # cliente Valkey-compatible
# redbeat no tiene wheels ARM64/Py3.12 — usar celery beat file-based en dev

# Generación de PDF — boletas (FASE 3)
weasyprint==63.1
jinja2==3.1.5

# Certificación digital Ed25519 + QR (FASE 27)
qrcode[pil]==8.1
web3==6.19.0

# IA — Asistente pedagógico (FASE 4)
anthropic==0.52.0
langchain==0.3.25
langchain-anthropic==0.3.15
langchain-community==0.3.24
openai==1.56.0
hvac==2.3.0

# Monitoreo — Prometheus métricas (FASE 22)
prometheus-fastapi-instrumentator==7.1.0
prometheus-client==0.21.1
