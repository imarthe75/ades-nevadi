# ADES Backend — FastAPI FASE 1
# Python 3.12

# Web framework
fastapi==0.139.2
uvicorn[standard]==0.34.0

# Base de datos (async + sync para Celery tasks)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Validación y configuración
pydantic==2.10.4
pydantic-settings==2.7.0
pydantic[email]

# Autenticación OIDC/JWT
python-jose[cryptography]==3.5.0
pyasn1==0.6.3           # pin directo: python-jose 3.4.0 exigía <0.5.0 (bloqueaba el fix de
                         # PYSEC-2026-2263); 3.5.0 relaja a >=0.5.0. ecdsa (transitivo de
                         # python-jose, PYSEC-2026-1325 sin fix upstream) no se pinea porque
                         # su ruta vulnerable (firmas ES256/ES384) nunca se ejecuta aquí —
                         # security.py fija algorithms=["RS256"] (ver hallazgo 2026-07-06).
httpx==0.28.1          # introspección JWKS de Authentik
xmltodict>=0.13.0      # parseo XML→dict para API BBB

# MinIO (almacenamiento S3)
minio==7.2.15

# Utilidades
python-multipart==0.0.31   # file uploads
openpyxl==3.1.5            # importación masiva Excel (.xlsx)
orjson==3.11.6             # JSON rápido
structlog==24.4.0          # logging estructurado

# Tareas async (Celery — worker separado)
celery[redis]==5.4.0
redis==5.2.1               # cliente Valkey-compatible
flower==2.0.1
# redbeat no tiene wheels ARM64/Py3.12 — usar celery beat file-based en dev

# Generación de PDF — boletas (FASE 3)
weasyprint==68.0
jinja2==3.1.6

# Certificación digital Ed25519 + QR (FASE 27)
qrcode[pil]==8.1
web3==6.19.0

# IA — Asistente pedagógico (FASE 4) — NVIDIA NIM (OpenAI API compatible)
# langchain/langchain-community removidos 2026-07-06: nunca se importan (llm_service.py
# usa el cliente openai directo), eran dead weight con 4+ CVEs sin mitigar (auditoría).
openai==1.56.0
hvac==2.3.0

# Monitoreo — Prometheus métricas (FASE 22)
prometheus-fastapi-instrumentator==8.0.2
prometheus-client==0.21.1
python-magic>=0.4.27
slowapi>=0.1.5
